<!DOCTYPE html>
<html lang='en'>
<head>
    <meta charset='UTF-8'>
    <meta name='viewport' content='width=device-width, initial-scale=1.0'>
    <title>My Applications - TutorLink Teacher</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel='stylesheet' href='/css/app.css'>
</head>
<body class='dashboard-page bg-light'>
    <nav class='nav shell' style='display:flex; justify-content:space-between; align-items:center; padding: 1rem 2rem; background:white; border-bottom:1px solid #eaeaea;'>
        <a class='brand' href='/' style='text-decoration:none; font-weight:bold; color:black;'><span class='brand-mark' style='background:black; color:white; padding:2px 6px; margin-right:5px; border-radius:4px;'>T</span>TutorLink</a>
        <div class='user-nav' style='display:flex; gap:1rem; align-items:center;'>
            <a href="/teacher" class="btn btn-outline-secondary btn-sm">Dashboard</a>
            <a href="/teacher/sessions" class="btn btn-outline-primary btn-sm">Browse Sessions</a>
            <b class='role-pill teacher-pill' style='background:#cff4fc; color:#055160; padding:4px 8px; border-radius:12px; font-size:0.8rem;'>Teacher</b>
            <span><strong><%= user.username %></strong></span>
            <a class='btn btn-danger btn-sm' href='/logout'>Log out</a>
        </div>
    </nav>
    <main class='dashboard shell container mt-4'>
        <% if (errorMessages.length) { %><div class='alert alert-danger'><%= errorMessages.join(' ') %></div><% } %>
        <% if (successMessages.length) { %><div class='alert alert-success'><%= successMessages.join(' ') %></div><% } %>
        
        <div class="mb-4">
            <h2>My Applications</h2>
            <p class="text-muted">Manage your teaching commitments and applications.</p>
        </div>

        <div class="table-responsive bg-white rounded shadow-sm">
            <table class="table table-hover align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Subject</th>
                        <th>Location</th>
                        <th>Date & Time</th>
                        <th>Status</th>
                        <th>Applied On</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (applications.length === 0) { %>
                        <tr><td colspan="6" class="text-center text-muted py-5">You haven't applied to any sessions yet. <a href="/teacher/sessions">Browse Sessions</a></td></tr>
                    <% } else { %>
                        <% applications.forEach(function(app) { %>
                            <tr>
                                <td><strong><%= app.subject %></strong></td>
                                <td><%= app.location %></td>
                                <td>
                                    <%= new Date(app.session_date).toLocaleDateString() %> <br>
                                    <small class="text-muted"><%= app.session_time %></small>
                                </td>
                                <td>
                                    <% if (app.status === 'approved') { %>
                                        <span class="badge bg-success px-2 py-1">Approved</span>
                                    <% } else if (app.status === 'applied') { %>
                                        <span class="badge bg-warning text-dark px-2 py-1">Pending</span>
                                    <% } else { %>
                                        <span class="badge bg-secondary px-2 py-1"><%= app.status %></span>
                                    <% } %>
                                </td>
                                <td><small class="text-muted"><%= new Date(app.applied_at).toLocaleDateString() %></small></td>
                                <td>
                                    <form action="/teacher/applications/<%= app.application_id %>/optout" method="POST" onsubmit="return confirm('Are you sure you want to withdraw your application?');">
                                        <button type="submit" class="btn btn-sm btn-outline-danger">Opt Out</button>
                                    </form>
                                </td>
                            </tr>
                        <% }); %>
                    <% } %>
                </tbody>
            </table>
        </div>
    </main>
</body>
</html>
